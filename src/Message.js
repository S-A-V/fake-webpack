export default class Message {
  static log(message) {
    console.log(message);
  }

  static info(message) {
    console.info(message);
  }

  static warning(message) {
    console.warn(message);
  }

  static clear() {
    console.clear();
  }
}
